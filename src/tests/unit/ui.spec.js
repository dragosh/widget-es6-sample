/**
* ------------------------------------------------------------------------
* Test the UI exporting public API
* ------------------------------------------------------------------------
*/
import * as Widget from '../../index'

describe('Widget', () => {

    it('should be a export an object', () => {
        expect(Widget).toBeObject()
    })
})
